local window = library:AddWindow("Script name here", {
	main_color = Color3.fromRGB(41, 74, 122), -- When ever this is changed The color of the Ui theme also changes like the uistroke or button or toggle thing etc
	main_size = Vector2.new(600, 340), -- Size of the gui
	Key_system = false
	Key = "Your key here"
})

local Main = window:AddTab("Tab name here") -- Name of tab

features:AddButton("Button name here",function()
    -- your code here
end)

local Toggle = features:AddToggle("Toggle name here", function()
	-- your code here
end)

Main:AddTextBox("Textbox name here", function(text)
	-- your code here
end)

local dropdown = Main:AddDropdown("Dropdown name her", function(text)
	-- Your code here
end)
local Option1 = dropdown:Add("Option1")  -- Options 
local Option2 = dropdown:Add("Option2")
local Option3 = dropdown:Add("Option3")

local Keybind = Main:AddKeybind("Keybind name here", {

Main:AddLabel("Your text here")

local slider = Main:AddSlider("Slider name here", function()
	-- Your code here  
end, {                    

	["min"] = 16,
	["max"] = 100,  
})
slider:Set(16) -- Needed
